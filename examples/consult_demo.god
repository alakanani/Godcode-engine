# Consult demo — CONSULT lays a question before the local Spirit oracle
# and receives two to three sentences of counsel. No external calls;
# the oracle lives inside the engine.
BEGIN CREATION
  REVEAL(CONSULT("How should I structure this covenant?"))
  REVEAL(CONSULT("What is the wisest next step for this work?"))
END CREATION
