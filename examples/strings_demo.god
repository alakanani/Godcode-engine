# Strings Demo — the scroll of strings: SHOUT, WHISPER, WORDS.
BEGIN CREATION
  IMPORT "strings"
  DECLARE greeting AS "peace be with you"
  REVEAL(SHOUT(greeting))
  REVEAL(WHISPER("LOUD PRAISE"))
  REVEAL(WORDS(greeting))
  ASCEND
END CREATION
