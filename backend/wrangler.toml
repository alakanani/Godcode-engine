name = "godcode-registry"
main = "worker.js"
compatibility_date = "2026-09-01"

# Deploys are currently done via the Cloudflare dashboard.
# Once the account can issue API tokens, fill in database_id and use `wrangler deploy`.
# [[d1_databases]]
# binding = "DB"
# database_name = "godcode_registry"
# database_id = ""

[vars]
# PUBLISH_TOKEN is set as an encrypted secret in the dashboard (Settings → Variables and Secrets).
